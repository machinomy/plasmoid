import Plasmoid from '../build/wrappers/Plasmoid'
import StandardToken from '../build/wrappers/StandardToken'
import Depositable from '../build/wrappers/Depositable'

export {
  Plasmoid,
  StandardToken,
  Depositable
}
